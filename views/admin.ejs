<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Gestión de Productos</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --color-bg: #fffdfb;
      --color-card: #ffffff;
      --color-accent: #b87333;
      --color-accent-hover: #9f5f28;
      --color-shadow: rgba(0, 0, 0, 0.08);
      --color-text: #222;
      --color-border: #e6e3e3;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--color-bg);
      padding: 40px 20px;
      color: var(--color-text);
    }

    .header {
      max-width: 1200px;
      margin: 0 auto 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header h1 {
      font-size: 2.2rem;
      color: var(--color-accent);
    }

    .btn-nuevo {
      text-decoration: none;
      background-color: var(--color-accent);
      color: #fff;
      padding: 10px 18px;
      border-radius: 30px;
      font-size: 0.95rem;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: background 0.3s ease;
    }

    .btn-nuevo:hover {
      background-color: var(--color-accent-hover);
    }

    .grid-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 24px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .card {
      background: var(--color-card);
      border: 1px solid var(--color-border);
      border-radius: 16px;
      box-shadow: 0 4px 12px var(--color-shadow);
      overflow: hidden;
      transition: transform 0.2s ease;
    }

    .card:hover {
      transform: translateY(-6px);
    }

    .card img {
      width: 100%;
      height: 160px;
      object-fit: cover;
      border-bottom: 1px solid #eee;
    }

    .card-body {
      padding: 14px 16px;
    }

    .card-body h2 {
      font-size: 1rem;
      margin-bottom: 4px;
      color: var(--color-accent);
      font-weight: 600;
    }

    .card-body p {
      font-size: 0.85rem;
      margin-bottom: 3px;
    }

    .card-body .precio {
      font-weight: bold;
      color: #000;
      margin: 6px 0;
    }

    .acciones {
      display: flex;
      justify-content: flex-end;
      gap: 10px;
      margin-top: 10px;
    }

    .acciones a {
      text-decoration: none;
      background-color: #f3f3f3;
      border-radius: 50%;
      padding: 8px;
      transition: background 0.3s ease;
    }

    .acciones a:hover {
      background-color: #ddd;
    }

    .acciones i {
      font-size: 14px;
    }

    .edit {
      color: #2e7d32;
    }

    .delete {
      color: #c62828;
    }

    @media (max-width: 600px) {
      .header {
        flex-direction: column;
        gap: 16px;
        align-items: flex-start;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><i class="fas fa-shoe-prints"></i> Lista de Zapatos</h1>
    <a class="btn-nuevo" href="/create">
      <i class="fas fa-plus"></i> Registrar Producto
    </a>
  </div>

  <div class="grid-container">
    <% productos.forEach(producto => { %>
      <div class="card">
        <img src="/uploads/<%= producto.imagen %>" alt="<%= producto.producto %>">
        <div class="card-body">
          <h2><%= producto.producto %></h2>
          <p><strong>Descripción:</strong> <%= producto.descripcion %></p>
          <p><strong>Talla:</strong> <%= producto.talla %> | <strong>Color:</strong> <%= producto.color %></p>
          <p><strong>Cantidad:</strong> <%= producto.cantidad %></p>
          <p class="precio">S/ <%= Number(producto.precio).toFixed(2) %></p>

          <div class="acciones">
            <a href="/edit/<%= producto.id %>" title="Editar"><i class="fas fa-pen edit"></i></a>
            <a href="/delete/<%= producto.id %>" class="delete-btn" title="Eliminar">
              <i class="fas fa-trash delete"></i>
            </a>
          </div>
        </div>
      </div>
    <% }); %>
  </div>


  

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</body>
</html>
